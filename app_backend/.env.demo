# For running this service directly with `bun index.ts` (outside Docker).
PORT=3000
FASTAPI_URL=http://localhost:8000
REDIS_URL=redis://localhost:6379
FRONTEND_URL=http://localhost:5173

# Reserved for future auth/email features (not currently used by any route).
JWT_SECRET=
DATABASE_URL=
GMAIL_USER=
GMAIL_APP_PASSWORD=