# NOTE: The Docker image for this service is built with `uv` directly from
# pyproject.toml + uv.lock (see Dockerfile) — this file is NOT used by Docker.
# It is provided only for environments that install with plain pip.
# Only the actual direct dependencies used by this codebase are listed below;
# pip will resolve the correct transitive packages automatically.
# (Run `uv export --no-dev -o requirements.txt` from this folder any time
# you want a fully pinned, reproducible version instead.)

celery>=5.6.3
fastapi[standard]>=0.136.1
huggingface-hub>=1.14.0
json-repair>=0.59.10
langchain>=1.3.0
langchain-community>=0.4.1
langchain-huggingface>=1.2.2
langchain-mistralai>=1.1.5
langchain-qdrant>=1.1.0
pymupdf>=1.27.2.3
python-dotenv>=1.2.2
qdrant-client>=1.18.0
redis>=7.4.0
sentence-transformers>=5.5.0
tavily-python>=0.7.26
uvicorn>=0.46.0
